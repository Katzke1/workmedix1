<%- include('../partials/head') %>
<body>
<div class="app-body">
<%- include('../partials/portal-sidebar') %>
<main class="app-main">
  <div class="app-topbar">
    <div class="app-topbar-title">
      <h2>My Bookings</h2>
      <p>All your screening appointments</p>
    </div>
    <div class="topbar-actions">
      <a href="/portal/book" class="btn btn-primary btn-sm">+ New Booking</a>
    </div>
  </div>

  <div class="app-content">
    <div class="card">
      <div class="card-header">
        <h3>Appointment History</h3>
        <span style="font-size:.85rem;color:var(--mid-grey);"><%= bookings.length %> booking<%= bookings.length !== 1 ? 's' : '' %></span>
      </div>
      <div class="table-wrap">
        <table>
          <thead>
            <tr>
              <th>#</th>
              <th>Service</th>
              <th>Date</th>
              <th class="hide-mobile">Location</th>
              <th>Status</th>
              <th class="hide-mobile">Submitted</th>
            </tr>
          </thead>
          <tbody>
            <% if (bookings.length === 0) { %>
              <tr>
                <td class="table-empty" colspan="6">
                  No bookings yet.
                  <a href="/portal/book">Request your first screening →</a>
                </td>
              </tr>
            <% } else { bookings.forEach((b, i) => { %>
              <tr>
                <td style="color:var(--mid-grey);font-size:.85rem;"><%= bookings.length - i %></td>
                <td><strong><%= b.service_type %></strong>
                  <% if (b.num_people > 0) { %><br><span style="font-size:.78rem;color:var(--mid-grey);"><%= b.num_people %> person<%= b.num_people !== 1 ? 's' : '' %></span><% } %>
                </td>
                <td style="white-space:nowrap;"><%= b.scheduled_at ? b.scheduled_at.split('T')[0] : b.preferred_date %></td>
                <td class="hide-mobile" style="color:var(--text-light);font-size:.85rem;max-width:180px;">
                  <%= b.location_text ? b.location_text.split('|')[0].trim().substring(0, 40) + (b.location_text.length > 40 ? '…' : '') : '—' %>
                </td>
                <td><span class="badge badge-<%= b.status %>"><%= b.status.replace('_',' ') %></span></td>
                <td class="hide-mobile" style="color:var(--mid-grey);font-size:.85rem;white-space:nowrap;">
                  <%= new Date(b.created_at).toLocaleDateString('en-ZA') %>
                </td>
              </tr>
            <% }) } %>
          </tbody>
        </table>
      </div>
    </div>

    <!-- Status legend -->
    <div class="card" style="margin-top:1.5rem;">
      <div class="card-body">
        <h4 style="color:var(--primary);margin-bottom:1rem;">Booking Status Guide</h4>
        <div style="display:flex;gap:2rem;flex-wrap:wrap;">
          <div style="display:flex;align-items:center;gap:.6rem;">
            <span class="badge badge-pending">pending</span>
            <span style="font-size:.88rem;color:var(--text-light);">Awaiting confirmation from Workmedix</span>
          </div>
          <div style="display:flex;align-items:center;gap:.6rem;">
            <span class="badge badge-confirmed">confirmed</span>
            <span style="font-size:.88rem;color:var(--text-light);">Appointment confirmed — check your email</span>
          </div>
          <div style="display:flex;align-items:center;gap:.6rem;">
            <span class="badge badge-in_progress">in progress</span>
            <span style="font-size:.88rem;color:var(--text-light);">Screening underway</span>
          </div>
          <div style="display:flex;align-items:center;gap:.6rem;">
            <span class="badge badge-completed">completed</span>
            <span style="font-size:.88rem;color:var(--text-light);">Screening done — results being processed</span>
          </div>
          <div style="display:flex;align-items:center;gap:.6rem;">
            <span class="badge badge-cancelled">cancelled</span>
            <span style="font-size:.88rem;color:var(--text-light);">Booking was cancelled</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>
</div>
<script src="/js/main.js"></script>
</body>
</html>
